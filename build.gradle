apply plugin: 'java'

repositories {
    mavenCentral()
    mavenLocal()
}

targetCompatibility = "1.6"
sourceCompatibility = "1.6"

dependencies {
    compile 'org.apache.cassandra:cassandra-all:1.1.6'
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.3'
}
